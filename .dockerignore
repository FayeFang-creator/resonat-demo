# 构建上下文只需要 backend/ + frontend 源码 + serve.py。
# 不排除的话 node_modules(119M) / .venv(36M) / data(30M) 全被打包上传，构建又慢又大。
**/node_modules
**/.venv
**/__pycache__
**/*.pyc
**/.pytest_cache
frontend/dist

# 密钥与运行时状态绝不进镜像
.env
.env.*
!.env.sample
backend/memory/*.md
backend/profile/memory/*.md
backend/profile/memory/*.json
backend/profile/*.db
backend/profile/analysis

# 主办方数据包（赛后须删除，见 CHALLENGE_AGREEMENT.md §2）——运行时也不再需要
data/

# 演示不需要的文档与素材
.git
.github
guides/
notebooks/
docs/
*.md
!README.md
