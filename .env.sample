# 复制成 `.env` 填你自己的值；`.env` 已被 git 忽略。
# 演示本身【不需要任何密钥】——赞助商 Cyanite API 赛后已下线，检索改为本地曲库。
# 下面全是可选增强，留空即走确定性兜底。

# 可选：开启 LLM 版意图解读与推荐解释
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-nano

# 可选：开启高音质下载代理；重建曲库（backend/build_catalog.py）时必填。
# 这是 Jamendo 的【公开】标识符，不是密钥，可以直接当普通环境变量配到部署平台。
JAMENDO_CLIENT_ID=
